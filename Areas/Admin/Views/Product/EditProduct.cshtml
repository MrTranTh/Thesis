@model Thesis.Models.Entity.Product
@using Microsoft.AspNetCore.Mvc.Rendering;

@{
    ViewData["Title"] = "Cập nhật sản phẩm";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";

}

<div class="pcoded-content">
<!-- [ breadcrumb ] start -->
    <div class="page-header">
        <div class="page-block">
            <div class="row align-items-center">
                <div class="col-md-12">
                    <div class="page-header-title">
                        <h5 class="m-b-10">Cập nhật</h5>
                    </div>
                    <ul class="breadcrumb">
                        <li class="breadcrumb-item"><a href="index.html"><i class="feather icon-home"></i></a></li>                          
                        <li class="breadcrumb-item"><a href="/Admin/Product">Quản lý sản phẩm</a></li>
                        <li class="breadcrumb-item"><a href="#">Cập nhật</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
    <!-- [ breadcrumb ] end -->
    <!-- [ Main Content ] start -->

    <div class="col-sm-12">
        @using (Html.BeginForm("EditProduct", "Product", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(false)
            @Html.HiddenFor(x=>x.Id)
            @Html.HiddenFor(x=>x.CreatedDate)
                <div class="card">
                    <div class="card-body">
                        <ul class="nav nav-pills mb-3" id="pills-tab" role="tablist">
                            <li class="nav-item">
                                <a class="nav-link active" id="pills-home-tab" data-toggle="pill" href="#pills-home" role="tab" aria-controls="pills-home" aria-selected="true">Thông tin chung</a>
                            </li>
                            @* <li class="nav-item">
                                <a class="nav-link" id="pills-profile-tab" data-toggle="pill" href="#pills-profile" role="tab" aria-controls="pills-profile" aria-selected="false">Hình ảnh</a>
                            </li> *@
                            <li class="nav-item">
                                <a class="nav-link" id="pills-contact-tab" data-toggle="pill" href="#pills-contact" role="tab" aria-controls="pills-contact" aria-selected="false">SEO</a>
                            </li>
                        </ul>

                        <div class="tab-content" id="pills-tabContent">
                            <!--Tab 1-->
                            <div class="tab-pane fade show active" id="pills-home" role="tabpanel" aria-labelledby="pills-home-tab">                        	
                                <div class="form-group">
                                    <label for="inputEmail4">Tên sản phẩm</label>
                                    @Html.TextBoxFor(x => x.Title, new {@class = "form-control", @placeholder="Tên sản phẩm" , @id="titleSanPham"})
                                    @Html.ValidationMessageFor(x => x.Title, null,  new{@class="text-danger"})
                                </div>
                                <div class="form-group">
                                     <label for="inputAddress">Alias</label>
                                     @Html.TextBoxFor(x => x.Alias, new {@class = "form-control", @placeholder="Alias", @id="aliasSanPham"})
                                </div>
                                <div class="form-group">
                                    <label for="inputEmail4">Danh mục</label>
                                    @Html.DropDownListFor(x=>x.ProductCategoriesId, ViewBag.ProductCategories as SelectList, "< Vui lòng chọn danh mục sản phẩm >",new {@class = "form-control"})                                  
                                </div>
                                <div class="form-group">
                                    <label for="inputAddress">Mã</label>
                                    @Html.TextBoxFor(x => x.ProductCode, new {@class = "form-control", @placeholder="Mã sản phẩm"})
                                </div>
                                <div class="form-group">
                                    <label for="inputAddress">Mô tả</label>
                                    @Html.TextAreaFor(x => x.Description, new {@class = "form-control", @placeholder="Mô tả sản phẩm"})
                                </div>
                                <div class="form-group">
                                    <label for="inputAddress">Chi tiết</label>
                                    @Html.TextAreaFor(x => x.Detail, new {@class = "form-control", @id ="txtDetail"})
                                </div>
                                <div class="form-group">
                                    <label for="inputAddress">Số lượng</label>
                                    @Html.TextBoxFor(x => x.Quantity, new {@class = "form-control", @placeholder="Số lượng sản phẩm"})
                                </div>
                                <div class="form-row">
                                    <div class="form-group col-md-6">
                                        <label for="inputEmail4">Giá thường</label>
                                        @Html.TextBoxFor(x => x.Price, new {@class = "form-control", @placeholder="Giá thường"})                                    
                                    </div>
                                    <div class="form-group col-md-6">
                                        <label for="inputEmail4">Giá khuyến mãi</label>
                                        @Html.TextBoxFor(x => x.PriceSale, new {@class = "form-control", @placeholder="Giá khuyến mãi"})                                    
                                    </div>
                                </div>
                                <div class="form-group col-md-4">
                                    @Html.CheckBoxFor(x => x.isHot, new{@class="form-check-input", @id="isHotCheckbox", @checked="checked"})
                                    <label for="isHotCheckbox">Hot</label>                               
                                </div>
                                <div class="form-group col-md-4">
                                    @Html.CheckBoxFor(x => x.isFeature, new{@class="form-check-input", @id="isFeatureCheckbox", @checked="checked"})
                                    <label for="isFeatureCheckbox">Nổi bật</label>                               
                                </div>        
                                <div class="form-group col-md-4">
                                    @Html.CheckBoxFor(x => x.isSale, new{@class="form-check-input", @id="isSaleCheckbox", @checked="checked"})
                                        <label for="isSaleCheckbox">Khuyến mãi</label>                               
                                </div>
                                <div class="form-group col-md-4">
                                    @Html.CheckBoxFor(x => x.isActive, new{@class="form-check-input", @id="inputAddress"})
                                    <label for="inputAddress">Hiển thị</label>                              
                                </div>  
                               <div class="form-group col-md-4">
                                    @Html.CheckBoxFor(x => x.isHome, new{@class="form-check-input", @id="inputAddress"})
                                     <label for="inputAddress">Hiển thị Trang chủ</label>                              
                                </div> 
                            </div>
                            <!--Tab 2-->
                            @* <div class="tab-pane fade" id="pills-profile" role="tabpanel" aria-labelledby="pills-profile-tab">
                                <div class="col-md-12">
                                    <div class="card">
                                        <div class="card-header">                                      
                                            <h5>Thư viện hình ảnh</h5>
                                            <button type="button" class="btn  btn-danger" id="btnChonAnh" onclick="">Tải ảnh</button>                                                                           
                                        </div>
                                        <div class="card-body table-border-style">
                                            <div class="table-responsive">
                                                <table class="table" id ="tableAnhSanPham">
                                                    <thead>
                                                        <tr>
                                                            <th class="text-center">Mã số</th>
                                                            <th class="text-center">Hình ảnh</th>
                                                            <th class="text-center">Ảnh đại diện</th>
                                                            <th class="text-center">Chức năng</th>
                                                        </tr>
                                                    </thead>
                                                    <tbody id="tbodyHtml">

                                                    </tbody>
                                                </table>
                                                <input type="hidden" id="tCurrentId" value="0">
                                            </div>
                                        </div>
                                    </div>
                                    </div>    
                            </div> *@
                            <!--Tab 3-->
                            <div class="tab-pane fade" id="pills-contact" role="tabpanel" aria-labelledby="pills-contact-tab">
                                <div class="form-group">
                                    <label for="inputAddress">Seo Title</label>
                                    @Html.TextBoxFor(x => x.SeoTitle, new {@class = "form-control", @placeholder="Seo Title"})
                                </div>
                                <div class="form-group">
                                    <label for="inputAddress2">Seo Description</label>
                                    @Html.TextAreaFor(x => x.SeoDescription, new {@class = "form-control",@placeholder="Seo Description"})                   
                                </div>
                                <div class="form-group">
                                    <label for="inputAddress2">Seo Keyword</label>
                                    @Html.TextBoxFor(x => x.SeoKeywords, new {@class = "form-control", @placeholder="Seo Keyword"})
                                </div>
                            </div>
                        </div>
                        <a href="/Admin/Product" class="btn  btn-danger">Quay lại</a>
                        <button type="submit" class="btn  btn-primary">Cập nhật</button>
                    </div>
                </div>
        }
    </div>

<!-- [ Main Content ] end -->
</div>
@section scripts{
    <script>       
        //Tạp plugin elfinder trong Summernote
            (function (factory) {
                if (typeof define === 'function' && define.amd) {
                    define(['jquery'], factory);
                } 
                else if (typeof module === 'object' && module.exports) {
                    module.exports = factory(require('jquery'));
                } else {
                    factory(window.jQuery);
                }
            }
            (function ($) {
                $.extend($.summernote.plugins, {
                    // Tạo plugin tên elfinder  
                    'elfinder': function (context) {
                        var self = this;
                        // ui has renders to build ui elements.
                        var ui = $.summernote.ui;
                        // Tạo nút bấm
                        context.memo('button.elfinder', function () {
                            var button = ui.button({
                                contents: '<i class="note-icon-picture"/> elFinder',
                                tooltip: 'Quản lý file',
                                click: function () {
                                    // Bấm vào nút bấm gọi hàm elfinderDialog   
                                    elfinderDialog(context); 
                                }
                            });
                            // create jQuery object from button instance.
                            var $elfinder = button.render();
                            return $elfinder;
                        });
                        // This methods will be called when editor is destroyed by $('..').summernote('destroy');
                        // You should remove elements on `initialize`.
                        this.destroy = function () {
                            this.$panel.remove();
                            this.$panel = null;
                        };
                    }

                });
            }));
            function elfinderDialog(editor) {
                var fm = $('<div/>').dialogelfinder({
                baseUrl: "@Url.Content("~/lib/elfinder/")",
                url: "@Url.Action("Connector","FileManager", new{area="Files"})",
                lang : 'vi',
                width : 840,
                height: 450,
                destroyOnClose : true,
                getFileCallback : function(files, fm) {
                    console.log(files);
                            editor.invoke('editor.insertImage', files.url);
                },
                commandsOptions : {
                    getfile : {
                        oncomplete : 'close',
                        folders : false
                    }
                }
                }).dialogelfinder('instance');
            }
            //Tạo Summernoter
            $(document).ready(function(){
                $('#txtDetail').summernote({
                height: 120,
                toolbar: [
                    ['style', ['style']],
                    ['font', ['bold', 'underline', 'clear']],
                    ['color', ['color']],
                    ['para', ['ul', 'ol', 'paragraph']],
                    ['table', ['table']],
                    ['insert', ['link', 'picture', 'video', 'elfinder']],
                    ['view', ['fullscreen', 'codeview', 'help']]
                ]
                });
            });

        //Tạo alias tự động
        $(document).ready(function () {
            $('#titleSanPham').on('keyup', function () {
                var tieuDe = $(this).val();
                var alias = chuyenDoiAlias(tieuDe);
                $('#aliasSanPham').val(alias);
            });

            function chuyenDoiAlias(tieuDe) {
                // Chuyển tiếng Việt có dấu thành không dấu
                var tmp = tieuDe
                    .toLowerCase()
                    .normalize("NFD")
                    .replace(/[\u0300-\u036f]/g, "");

                // Xóa các ký tự không mong muốn
                var alias = tmp.replace(/[^a-z0-9\s]/g, '');

                // Thay thế khoảng trắng bằng dấu "-"
                alias = alias.replace(/\s+/g, '-');

                // Loại bỏ các dấu "-" liên tiếp và dấu "-" ở đầu và cuối chuỗi
                alias = alias.replace(/-+/g, '-').replace(/^-+|-+$/g, '');

                return alias;
            }
        });
            
    </script>
}

